/*
require('jest')
const app = require('.././index');
const { pets } = require('../data');
const request = requi


describe('Unit test for pets controller', ()=> {
    describe('GET', ()=>{
        it('should get all pets', async ()=> {
            const res = await request(app).get('/pets').expact(200)
            expect(pets).toEqual(res);
        });
    })
});
*/